# =============================================================================
# Airymax Docker .gitignore
# 版本: 0.1.0
# 最后更新: 2026-06-19
# 用途: 排除 Docker 构建产物、环境配置及本地开发临时文件
# 所有权: SPHARX Ltd
# Part of Airymax open-source project
# =============================================================================

# -----------------------------------------------------------------------------
# 环境变量文件（包含敏感信息，绝对不能提交）
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local
.env.dev
.env.test
.env.production
.env.staging

# 环境模板（保留）
!.env.example
!.env.staging.example
!.env.production.example

# -----------------------------------------------------------------------------
# Docker生成的文件和目录
# -----------------------------------------------------------------------------
docker/data/
docker/logs/

# Docker卷数据（本地持久化）
data/
*.tar.gz
*.tar
*.sql

# 数据库文件
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Docker Compose 覆盖文件
docker-compose.override.yml

# -----------------------------------------------------------------------------
# 证书和密钥（绝对不能提交）
# -----------------------------------------------------------------------------
*.pem
*.key
*.crt
*.csr
*.p12
*.jks
keystore/
ssl/
certs/

# -----------------------------------------------------------------------------
# 许可证文件（商业密钥，绝对不能提交）
# -----------------------------------------------------------------------------
*.lic

# -----------------------------------------------------------------------------
# Docker Secrets 目录（仅保留模板和文档）
# -----------------------------------------------------------------------------
secrets/*.txt
secrets/*.key
secrets/*.pem
secrets/*.lic
!secrets/*.template
!secrets/*.md

# -----------------------------------------------------------------------------
# 监控配置中的敏感信息
# -----------------------------------------------------------------------------
monitoring/grafana/provisioning/datasources/*.yaml
!monitoring/grafana/provisioning/datasources/datasource.yml.template

# -----------------------------------------------------------------------------
# 构建产物缓存
# -----------------------------------------------------------------------------
.docker/
build/
dist/
node_modules/
__pycache__/
*.pyc
*.pyo
.venv/
venv/
opt/venv/

# C/C++构建产物
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
out/
cmake-build-*/
*.o
*.obj
*.a
*.so
*.dylib

# Go构建产物
vendor/

# Rust构建产物
target/

# -----------------------------------------------------------------------------
# IDE和编辑器配置
# -----------------------------------------------------------------------------
.vscode/
.idea/
.trae/
*.swp
*.swo
*~
.DS_Store

# -----------------------------------------------------------------------------
# 日志文件
# -----------------------------------------------------------------------------
*.log
logs/
*.log.*

# -----------------------------------------------------------------------------
# 测试覆盖率报告
# -----------------------------------------------------------------------------
coverage/
.coverage
htmlcov/
.nyc_output/

# -----------------------------------------------------------------------------
# 临时文件
# -----------------------------------------------------------------------------
tmp/
temp/
*.tmp
*.temp
*.bak
*.backup

# -----------------------------------------------------------------------------
# Kubernetes 配置（敏感信息）
# -----------------------------------------------------------------------------
*.kubeconfig
.kube/

# -----------------------------------------------------------------------------
# 操作系统文件
# -----------------------------------------------------------------------------
Thumbs.db
Desktop.ini

# -----------------------------------------------------------------------------
# v3.1 补全 — 以下条目于 2026-07-01 补充以统一全项目 .gitignore 标准（Airymax）
# -----------------------------------------------------------------------------

# IDE 配置补全
*.code-workspace

# 编译产物补全
*.dll
*.exe

# Python 包管理产物补全
*.egg-info/

# Python 测试缓存补全
.pytest_cache/

# Rust 构建产物补全
# 本目录不含 Rust 源码，此规则为占位防护
Cargo.lock

# Go 测试二进制补全
*.test

# 编译输出补全
*.out