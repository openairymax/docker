# =============================================================================
# AgentOS Staging 环境变量配置模板
# 版本: 3.0.0 (Staging Environment)
# 最后更新: 2026-04-23
#
# 使用方法:
#   cp .env.staging.example .env.staging
#   vim .env.staging
#
# 注意:
#   - Staging 环境用于预发布验证，使用独立数据卷
#   - 密码可以使用固定值（非生产环境）
# =============================================================================

# -----------------------------------------------------------------------------
# 通用配置 (General)
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
TZ=Asia/Shanghai
LANG=C.UTF-8
LC_ALL=C.UTF-8

# AgentOS 版本号
AGENTOS_VERSION=3.0.0

# 日志配置
LOG_MAX_SIZE=50m
LOG_MAX_FILE=3

# -----------------------------------------------------------------------------
# Gateway 服务配置 (Gateway)
# -----------------------------------------------------------------------------
GATEWAY_PORT=18789
GATEWAY_ADMIN_PORT=18790

# JWT 密钥 (Staging 可用固定值)
GATEWAY_JWT_SECRET=staging_jwt_secret_2024_change_in_production_2026

# Token 配置
TOKEN_EXPIRY_HOURS=4
REFRESH_TOKEN_DAYS=2

# 速率限制 (Staging 较宽松)
GATEWAY_GLOBAL_QPS=10000
GATEWAY_PER_IP_QPS=200

# -----------------------------------------------------------------------------
# 内核服务配置 (Kernel)
# -----------------------------------------------------------------------------
KERNEL_IPC_PORT=18080
KERNEL_METRICS_PORT=9090

# 资源限制 (Staging 中等)
CPU_LIMIT=2
CPU_RESERVATION=0.5
MEMORY_LIMIT=2G
MEMORY_RESERVATION=512M

MAX_CONNECTIONS=5000
MEMORY_LIMIT_MB=4096
CPU_CORES=4

# HeapStore 配置
HEAPSTORE_MAX_LOG_SIZE_MB=200
HEAPSTORE_LOG_RETENTION_DAYS=14
HEAPSTORE_CIRCUIT_THRESHOLD=5
HEAPSTORE_CIRCUIT_TIMEOUT_SEC=30

# -----------------------------------------------------------------------------
# PostgreSQL 数据库配置 (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_DB=agentos_staging
POSTGRES_USER=agentos
POSTGRES_PASSWORD=staging_password_2024_change_me

# PostgreSQL 性能调优
PG_MAX_CONNECTIONS=100
PG_SHARED_BUFFERS=128MB
PG_EFFECTIVE_CACHE_SIZE=512MB
PG_WORK_MEM=4MB
PG_MAINTENANCE_WORK_MEM=32MB
PG_SLOW_QUERY_MS=500

# -----------------------------------------------------------------------------
# Redis 缓存配置 (Redis)
# -----------------------------------------------------------------------------
REDIS_PASSWORD=staging_redis_2024_change_me
REDIS_MAX_MEMORY=256mb

# -----------------------------------------------------------------------------
# Grafana 监控配置 (Grafana)
# -----------------------------------------------------------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin_staging_2024

# -----------------------------------------------------------------------------
# Staging 特定配置
# -----------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=agentos-staging
